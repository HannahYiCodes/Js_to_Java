function countVowels(str) {
    for (let i = 0; i < str.length; i++) {
        if (str[i] = 'a' && 'A'| 'e' && 'E' | 'i' && 'I'| 'o' && 'O' | 'u' && 'U') {
            console.log([i].length);
        }
    }
}
countVowels("Hannah")